<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.purchase.mapper.order.BizPurchaseOrderMapper" >
  <sql id="Update_By_Example_Where_Clause" >
    <where >
      <foreach collection="example.oredCriteria" item="criteria" separator="or" >
        <if test="criteria.valid" >
          <trim prefix="(" suffix=")" prefixOverrides="and" >
            <foreach collection="criteria.criteria" item="criterion" >
              <choose >
                <when test="criterion.noValue" >
                  and a.${criterion.condition}
                </when>
                <when test="criterion.singleValue" >
                  and a.${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue" >
                  and a.${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue" >
                  and a.${criterion.condition}
                  <foreach collection="criterion.value" item="listItem" open="(" close=")" separator="," >
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>

  <resultMap id="BaseResultMapExt" type="com.purchase.vo.order.BizPurchaseOrderVo" extends="BaseResultMap">
    <result column="supplier_name" property="supplierName" jdbcType="VARCHAR" />
    <result column="userFullName" property="userFullName" jdbcType="VARCHAR" />
  </resultMap>
  <select id="selectByExampleExt" resultMap="BaseResultMapExt">
    select
        a.id, a.purchase_no, a.type, a.create_user, a.create_time, a.supplier_id, a.project_id, a.contract_id,
        a.contract_money, a.payment_ratio, a.request_amount, a.payment_amount, a.status, a.update_date,
        a.summary, a.cost_depart_approval, a.cost_depart_user, a.cost_depart_date, a.cost_depart_opinion,
        a.project_depart_approval, a.project_depart_user, a.project_depart_date, a.project_depart_opinion,
        a.manager_depart_approval, a.manager_depart_user, a.manager_depart_date, a.manager_depart_opinion,
        b.name as supplier_name, c.fullname as userFullName
    from biz_purchase_order a
    INNER JOIN tb_supplier b ON (a.supplier_id = b.id)
    INNER JOIN tb_admin c ON (a.create_user = c.id)
    where 1 = 1
    <if test="_parameter != null" >
      <include refid="Example_Where_Clause_Ext" />
    </if>
    <if test="@import org.apache.commons.lang3.StringUtils@isNotBlank(search.createUserName)" >
      c.fullname LIKE concat(concat('%',#{search.createUserName}),'%')
    </if>
    <if test="orderByClause != null" >
      order by ${orderByClause}
    </if>
  </select>
</mapper>